# Test Configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.liquibase.enabled=true

# External API Configuration - will be overridden in tests
external.api.url=http://localhost:8888
external.api.scheduler.rate=1000

# Disable scheduling for most tests
spring.main.allow-bean-definition-overriding=true 